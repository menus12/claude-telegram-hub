{
  "name": "@claude-telegram-hub/channel",
  "version": "0.0.0",
  "description": "Thin Claude Code channel: injects hub messages into a session and relays replies back through the hub",
  "type": "module",
  "main": "./dist/main.cjs",
  "bin": {
    "claude-telegram-channel": "./dist/main.cjs"
  },
  "files": [
    "dist",
    ".claude-plugin",
    ".mcp.json"
  ],
  "scripts": {
    "build": "node build.mjs",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@claude-telegram-hub/protocol": "*",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@types/ws": "^8.5.12",
    "ws": "^8.18.0"
  }
}
